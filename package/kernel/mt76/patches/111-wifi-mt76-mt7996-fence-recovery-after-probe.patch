diff --git a/mt7996/init.c b/mt7996/init.c
index 5aada567..6d39f274 100644
--- a/mt7996/init.c
+++ b/mt7996/init.c
@@ -1763,6 +1763,9 @@ int mt7996_register_device(struct mt7996_dev *dev)
 	struct mt7996_phy *phy;
 	int ret;
 
+	WRITE_ONCE(dev->recovery_flags, 0);
+	dev->recovery.hw_init_done = false;
+
 	dev->phy.dev = dev;
 	dev->phy.mt76 = &dev->mt76.phy;
 	dev->mt76.phy.priv = &dev->phy;
@@ -1778,36 +1781,32 @@ int mt7996_register_device(struct mt7996_dev *dev)
 
 	ret = mt7996_init_hardware(dev);
 	if (ret)
-		return ret;
+		goto error;
 
 	mt7996_init_wiphy(hw, &dev->mt76.mmio.wed);
 
 	ret = mt7996_register_phy(dev, MT_BAND1);
 	if (ret)
-		return ret;
+		goto error;
 
 	ret = mt7996_register_phy(dev, MT_BAND2);
 	if (ret)
-		return ret;
+		goto error;
 
 	ret = mt7996_npu_hw_init(dev);
 	if (ret)
-		return ret;
+		goto error;
 
 	mt7996_dma_rro_start(dev);
 
 	ret = mt76_register_device(&dev->mt76, true, mt76_rates,
 				   ARRAY_SIZE(mt76_rates));
 	if (ret)
-		return ret;
+		goto error;
 
 	mt7996_for_each_phy(dev, phy)
 		mt7996_thermal_init(phy);
 
-	ieee80211_queue_work(mt76_hw(dev), &dev->init_work);
-
-	dev->recovery.hw_init_done = true;
-
 	ret = mt7996_init_debugfs(dev);
 	if (ret)
 		goto error;
@@ -1816,9 +1815,16 @@ int mt7996_register_device(struct mt7996_dev *dev)
 	if (ret)
 		goto error;
 
+	dev->recovery.hw_init_done = true;
+	set_bit(MT7996_RECOVERY_READY, &dev->recovery_flags);
+	ieee80211_queue_work(mt76_hw(dev), &dev->init_work);
+
 	return 0;
 
 error:
+	clear_bit(MT7996_RECOVERY_READY, &dev->recovery_flags);
+	dev->recovery.hw_init_done = false;
+	cancel_work_sync(&dev->reset_work);
 	cancel_work_sync(&dev->init_work);
 
 	return ret;
@@ -1826,6 +1832,9 @@ error:
 
 void mt7996_unregister_device(struct mt7996_dev *dev)
 {
+	clear_bit(MT7996_RECOVERY_READY, &dev->recovery_flags);
+	dev->recovery.hw_init_done = false;
+
 	cancel_work_sync(&dev->dump_work);
 	cancel_work_sync(&dev->wed_rro.work);
 	cancel_work_sync(&dev->reset_work);
diff --git a/mt7996/mac.c b/mt7996/mac.c
index 570f85d7..111c1caa 100644
--- a/mt7996/mac.c
+++ b/mt7996/mac.c
@@ -2466,6 +2466,10 @@ mt7996_mac_full_reset(struct mt7996_dev *dev)
 	LIST_HEAD(list);
 	int i;
 
+	if (!test_bit(MT7996_RECOVERY_READY, &dev->recovery_flags) ||
+	    !test_bit(MT76_STATE_REGISTERED, &dev->mphy.state))
+		return;
+
 	dev->recovery.hw_full_reset = true;
 
 	set_bit(MT76_MCU_RESET, &dev->mphy.state);
@@ -2484,8 +2488,13 @@ mt7996_mac_full_reset(struct mt7996_dev *dev)
 			break;
 	}
 
-	if (i == 10)
+	if (i == 10) {
 		dev_err(dev->mt76.dev, "chip full reset failed\n");
+		dev->recovery.hw_full_reset = false;
+		mutex_unlock(&dev->mt76.mutex);
+
+		return;
+	}
 
 	mt7996_for_each_phy(dev, phy)
 		phy->omac_mask = 0;
@@ -2520,8 +2529,13 @@ mt7996_mac_full_reset(struct mt7996_dev *dev)
 
 	mutex_unlock(&dev->mt76.mutex);
 
-	ieee80211_wake_queues(mt76_hw(dev));
+	if (!test_bit(MT7996_RECOVERY_READY, &dev->recovery_flags) ||
+	    !test_bit(MT76_STATE_REGISTERED, &dev->mphy.state))
+		return;
 
+	ieee80211_wake_queues(mt76_hw(dev));
+	dev_info(dev->mt76.dev,
+		 "firmware recovered, requesting mac80211 restart\n");
 	ieee80211_restart_hw(mt76_hw(dev));
 }
 
@@ -2535,6 +2549,11 @@ void mt7996_mac_reset_work(struct work_struct *work)
 	dev = container_of(work, struct mt7996_dev, reset_work);
 	hw = mt76_hw(dev);
 
+	if (!test_bit(MT7996_RECOVERY_READY, &dev->recovery_flags)) {
+		dev->recovery.restart = false;
+		return;
+	}
+
 	/* chip full reset */
 	if (dev->recovery.restart) {
 		/* disable WA/WM WDT */
@@ -2760,7 +2779,7 @@ skip_coredump:
 
 void mt7996_reset(struct mt7996_dev *dev)
 {
-	if (!dev->recovery.hw_init_done)
+	if (!test_bit(MT7996_RECOVERY_READY, &dev->recovery_flags))
 		return;
 
 	if (dev->recovery.hw_full_reset)
diff --git a/mt7996/mcu.c b/mt7996/mcu.c
index c8a9f316..d8ccbf99 100644
--- a/mt7996/mcu.c
+++ b/mt7996/mcu.c
@@ -229,7 +229,11 @@ mt7996_mcu_parse_response(struct mt76_dev *mdev, int cmd,
 		dev_err(mdev->dev, "Message %08x (seq %d) timeout\n",
 			cmd, seq);
 
-		if (!test_and_set_bit(MT76_MCU_RESET, &dev->mphy.state)) {
+		if (test_bit(MT7996_RECOVERY_READY, &dev->recovery_flags) &&
+		    test_bit(MT76_STATE_REGISTERED, &dev->mphy.state) &&
+		    !test_and_set_bit(MT76_MCU_RESET, &dev->mphy.state)) {
+			dev_warn(mdev->dev,
+				 "MCU timeout after probe, scheduling recovery\n");
 			dev->recovery.restart = true;
 			wake_up(&dev->mt76.mcu.wait);
 			queue_work(dev->mt76.wq, &dev->reset_work);
diff --git a/mt7996/mt7996.h b/mt7996/mt7996.h
index 673be6df..841ca47d 100644
--- a/mt7996/mt7996.h
+++ b/mt7996/mt7996.h
@@ -449,6 +449,8 @@ struct mt7996_phy {
 	bool rdd_tx_paused;
 };
 
+#define MT7996_RECOVERY_READY 0
+
 struct mt7996_dev {
 	union { /* must be first */
 		struct mt76_dev mt76;
@@ -485,6 +487,8 @@ struct mt7996_dev {
 	struct work_struct dump_work;
 	struct work_struct reset_work;
 	wait_queue_head_t reset_wait;
+	/* Recovery is enabled only after probe completed successfully. */
+	unsigned long recovery_flags;
 	struct {
 		u32 state;
 		u32 wa_reset_count;
